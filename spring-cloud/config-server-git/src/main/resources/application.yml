spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Liubangxin/config-repo-demo.git
server:
  port: 1201

encrypt:
  key: lbx

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1001/eureka/